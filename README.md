# AETG by ljx
 
#### there are 2 classes which is computed

### initialize the pairwise class and the threewise class, the result will be generated in _result.xlsx after executing two_wise() or three_wise() func